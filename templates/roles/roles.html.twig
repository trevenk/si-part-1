{% extends 'base.html.twig' %}

{% block title %} Roles {% endblock %}

{% block body %}
    <div  class="container justify-content-center text-center mt-5">

        <h1 class="text-center justify-content-center">&Aacute;rea de trabajo</h1>
        <br>
        <a href="{{ path('roles_show', {'slug': 'cocina'}) }}" class="badge badge-light">
            <div class="card" style="width: 256px;">
                <div class="card-body">
                    <h5 class="card-title">Cocina</h5>
                </div>
            </div>
        </a>
        <a href="{{ path('roles_show', {'slug': 'camarero'}) }}" class="badge badge-light">
            <div class="card" style="width: 256px;">
                <div class="card-body">
                    <h5 class="card-title">Camarero</h5>
                </div>
            </div>
        </a>
        <a href="{{ path('roles_show', {'slug': 'barra'}) }}" class="badge badge-light">
            <div class="card" style="width: 256px;">
                <div class="card-body">
                    <h5 class="card-title">Barra</h5>
                </div>
            </div>
        </a>
        <a href="{{ path('roles_show', {'slug': 'admin'}) }}" class="badge badge-light">
            <div class="card" style="width: 256px;">
                <div class="card-body">
                    <h5 class="card-title">Administraci&oacute;n</h5>
                </div>
            </div>
        </a>

    </div>

{% endblock %}
